{
  "name": "challan",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n be,fe -c blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
